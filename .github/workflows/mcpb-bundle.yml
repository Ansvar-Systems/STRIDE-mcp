name: Build MCPB Bundle

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  mcpb:
    uses: Ansvar-Systems/mcp-server-template/.github/workflows/mcpb-bundle-node.yml@e0d2aa7a273e1665bd06eac6eea522781e8f704e
    with:
      server-name: stride-patterns-mcp
